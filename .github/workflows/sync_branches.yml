# Generated by internal/ci/ci_tool.cue; do not edit
name: Sync Next Release
on:
  push:
    branches:
    - release/2.3.0
jobs:
  sync:
    name: Sync next release
    runs-on: ubuntu-20.04
    defaults:
      run:
        shell: bash
    if: ${{ github.ref == 'refs/heads/release/2.3.0' }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: pull-request-action
      uses: vsoch/pull-request-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        PULL_REQUEST_BRANCH: release/2.4.0
        PULL_REQUEST_TITLE: 'chore: ${{ github.ref }} to release/2.4.0'
        PULL_REQUEST_UPDATE: "true"
    - name: pull-request-action
      uses: vsoch/pull-request-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        PULL_REQUEST_BRANCH: develop
        PULL_REQUEST_TITLE: 'chore: ${{ github.ref }} to develop'
        PULL_REQUEST_UPDATE: "true"
    - name: pull-request-action
      uses: vsoch/pull-request-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        PULL_REQUEST_BRANCH: master
        PULL_REQUEST_TITLE: Release ${{ github.ref }}
        PULL_REQUEST_UPDATE: "true"
