# Generated by internal/ci/ci_tool.cue; do not edit
name: Sync Next Release
on:
  push:
    branches:
    - develop
    - release/2.3.0
jobs:
  sync:
    name: Sync next release
    runs-on: ubuntu-24.04
    defaults:
      run:
        shell: bash
    if: ${{ github.ref == 'refs/heads/release/2.3.0' || github.ref == 'refs/heads/develop'
      }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - if: ${{ github.ref == 'refs/heads/develop' }}
      name: pull-request-action
      uses: vsoch/pull-request-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        PULL_REQUEST_BRANCH: release/2.3.0
        PULL_REQUEST_TITLE: 'chore: develop to release/2.3.0'
        PULL_REQUEST_UPDATE: "true"
    - if: ${{ github.ref == 'refs/heads/release/2.3.0' }}
      name: pull-request-action
      uses: vsoch/pull-request-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        PULL_REQUEST_BRANCH: master
        PULL_REQUEST_TITLE: Release ${{ github.ref }}
        PULL_REQUEST_UPDATE: "true"
