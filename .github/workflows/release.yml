name: Release

jobs:
  deploy-release:
    name: Deploy Release
    runs-on: ubuntu-latest
    steps:

    - name: Check out code into the Go module directory
      uses: actions/checkout@v2

    - name: Push to Docker Hub
      uses: docker/build-push-action@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        dockerfile: build/Dockerfile
        registry: quay.io
        repository: rh-marketplace/redhat-marketplace-operator
        tag_with_ref: true

  deploy-develop:
    name: Release
    runs-on: ubuntu-latest
    steps:

    - name: Check out code into the Go module directory
      uses: actions/checkout@v2

    - name: Push to Docker Hub
      uses: docker/build-push-action@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        dockerfile: build/Dockerfile
        registry: quay.io
        repository: rh-marketplace/redhat-marketplace-operator
        tag_with_ref: true

  deploy-master:
    name: Deploy Master
    runs-on: ubuntu-latest
    steps:

    - name: Check out code into the Go module directory
      uses: actions/checkout@v2

    - name: Push to Docker Hub
      uses: docker/build-push-action@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        dockerfile: build/Dockerfile
        registry: quay.io
        repository: rh-marketplace/redhat-marketplace-operator
        tag_with_ref: true
