# Generated by internal/ci/ci_tool.cue; do not edit
name: Build Bases
on:
  workflow_dispatch: {}
  schedule:
  - cron: 0 0 * * *
jobs:
  base:
    name: Build Base
    runs-on: ubuntu-24.04
    env:
      GO_VERSION: "1.22"
      IMAGE_REGISTRY: quay.io/rh-marketplace
    defaults:
      run:
        shell: bash
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    - name: Install Go
      uses: actions/setup-go@v4
      with:
        go-version: "1.22"
        cache: false
    - name: Set up QEMU
      uses: docker/setup-qemu-action@v2
    - id: buildx
      name: Set up docker buildx
      uses: docker/setup-buildx-action@v2
      with:
        config: .github/buildkitd.toml
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        registry: quay.io/rh-marketplace
        username: ${{secrets['quayUser']}}
        password: ${{secrets['quayPassword']}}
    - id: build-data-service
      name: Build images
      run: make base/data-service
      env:
        DOCKERBUILDXCACHE: /tmp/.buildx-cache
        PUSH: "true"
        IMAGE_PUSH: "true"
    - id: build-data-service-ubi-minimal
      name: Build images
      run: make base/data-service-ubi-minimal
      env:
        DOCKERBUILDXCACHE: /tmp/.buildx-cache
        PUSH: "true"
        IMAGE_PUSH: "true"
