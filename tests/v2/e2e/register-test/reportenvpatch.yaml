spec:
  template:
    spec:
      containers:
        - name: manager
          env:
            - name: REPORT_RETRY_TIME_DURATION
              value: "2m"
            - name: REPORT_RETRY_LIMIT
              value: "2"
            - name: "METER_DEF_CONTROLLER_REQUEUE_RATE"
              value: "20s"
